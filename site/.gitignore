node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
# Generated by scripts/build-data.mjs from the repository root: results, datasets, harness,
# day-60 and charter. Rebuilt by `pnpm build`; never edited by hand.
data/benchmarks.json
# 1440px QA screenshots. Several megabytes of PNG that one command reproduces, and this
# repository is deliberately text-only.
qa/
# The source the repository root files were written from. Keeping a second copy of README.md,
# LAYOUT.md and the rest one directory down would be two files to keep in step and one of
# them would eventually be wrong.
repo-template/
